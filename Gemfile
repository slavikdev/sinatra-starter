# frozen_string_literal: true
source 'https://rubygems.org'

gem 'sinatra'
gem 'sinatra-contrib'
gem 'nokogiri', '1.7.2' # this version actually works
gem 'rake'

group :test do
  gem 'rspec'
  gem 'rack-test'
end
